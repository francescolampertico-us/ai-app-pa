[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3.12 -m pip install -r fastapi-backend/requirements.txt"]

[start]
cmd = "cd fastapi-backend && python3.12 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
