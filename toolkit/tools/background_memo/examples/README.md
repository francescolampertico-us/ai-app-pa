# Background Memo Examples

This folder contains example background memos used as style references for the tool.

**These files are excluded from version control** (see `.gitignore`) because they contain
client-confidential content. Do not commit any `.docx` or `.pdf` files from this folder.

Place your reference memos here. The tool will use them as style guides.
