# Examples (Redacted)

Public repo policy: do not store organization or client examples here.

If you need local examples for evaluation:
- keep them in `examples/final_outputs/` locally
- do not commit them (this path is gitignored)

Use `STYLE_REFERENCE_NOTES.md` as the public-facing style reference.
